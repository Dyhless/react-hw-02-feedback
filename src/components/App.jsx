import Feedback from './Step1/step1';

export const App = () => {
  return (
    <Feedback
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: 40,
        color: '#010101',
      }}
    />
  );
};
